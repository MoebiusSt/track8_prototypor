/** Shared device viewport and horizontal content extent for demo pages. */

export const VISIBLE_WIDTH = 1280;
export const VISIBLE_HEIGHT = 400;
/** Total scrollable width in pixels (piano roll content; waveform bar columns use BAR_PITCH). */
export const TOTAL_WIDTH = 5000;
/** One waveform column: 2px bar + 2px gap (background). */
export const BAR_PITCH = 4;
